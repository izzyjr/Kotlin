package kotlincode

class SortByHeight {

    fun sortByHeight(a: MutableList<Int>): MutableList<Int> {
        var tallest: Int = Int.MIN_VALUE
        for (i in 0 until a.lastIndex) {
            for (j in 0 until a.lastIndex) {
                if (a[i] != a[j]) {

                }
            }
        }

        return a
    }

}